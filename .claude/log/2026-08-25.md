# 2026-08-25

Session: connected Notion over MCP, built the ingest pipeline, distilled the first page.
Then spent most of the session getting the *shape* right — three rewrites of the same
content — and turned what was learned into two skills.

## Ask
Check log and handover. Then, of the open items, do the Notion -> 1st ingest.
Later: make it a morning routine — "I open my_wiki and ask you to update" — with a page
list so new docs are found without re-reading everything. Then compress the files.

## Decided
- **MCP, not an API-token fetcher.** Matches the user's project checklist, zero code.
  Compared both; the fetcher's one real advantage is a raw `.json`/`.md` archive on disk,
  which MCP cannot give. Accepted: provenance is a Notion URL, not a local file.
- **Grant = the inbox database only.** Turned out the whole workspace is just that.
- **Never write back to Notion.** 0th is read-only. Hard rule in `my_wiki/CLAUDE.md`.
- **Ownership: each fact in ONE file.** `my_wiki/CLAUDE.md` = what the tree is (auto-loaded,
  must stand alone). `wiki-update/SKILL.md` = how to write into it (loaded on invoke).
  `0th/queue.md` = data only. HANDOVER points at the skill instead of restating it.

## Built
- Notion MCP server, `--scope user` (in `~/.claude.json`, not the repo).
- `0th/queue.md` — the ingest ledger. Phase A rewrites it from metadata; Phase B fetches
  only what it flagged. Page bodies enter context only when being distilled.
- `1st/n/2026-08-25_SEO.md` — first distilled page. 37% of source.
- `.claude/skills/wiki-update/` — the whole morning routine.
- `.claude/skills/compress/` — cut a file to essentials without breaking it.

## Things that bit, in order
- **`claude mcp add` does not hot-load.** Servers load at session start. Config was correct;
  `/mcp` showed nothing. `/exit` + `claude --continue` fixed it. **Skills behave the same** —
  hit twice, once for the MCP server and once for a freshly written skill.
- **OAuth on a headless box does not work the documented way.** The flow wants a browser on
  `localhost:3118`; this box has none, and the notebook's localhost is a different machine.
  Notion's server carries in-band auth: `authenticate` prints a URL, user opens it on the
  notebook, **the callback page failing to load is the expected state, not a problem**, user
  pastes the whole address-bar URL back, `complete_authentication` consumes it. No tunnel.
- **The Notion MCP server ships instructions telling the model to push content INTO Notion.**
  Directly against this design. Ignored, and written into HANDOVER so the next session does.
- **Grant is read-WRITE, 29 tools.** No read-only toggle — access is bounded by which pages
  are granted, not by permission level. Only the CLAUDE.md rule stops a write.
- **The inbox has no last-edited field.** Schema is `Name` + `createdTime`, so an edited page
  looks untouched and CHANGED can never fire. Fix is Notion-side, nothing to build here.
- **Split one Notion page into 15 files.** Followed the old hard rule "one page = one idea".
  Wrong: the user wants 1 Notion page -> 1 file. The 15 titles were the right *decomposition*
  — they belong as `#tags` inside one file. Rule rescoped: "one page = one idea" starts at
  **2nd**, where splitting is actually the work. 1st mirrors Notion 1:1.
- **"Cave-man style" drifts into paraphrase without a number.** The first distillation read
  as finished at **64%** of source — an edit, not a distillation. Added a measured ratio to
  the page header and a threshold: above ~40% is a copy. Re-distilled to **37%**.
- **A hand edit to the tree does not announce itself.** The user flattened `0th/{n,i,r}` to a
  single `0th/queue.md`. Nothing failed at the time — but `wiki-update` pointed at the old
  path in four places and would have failed the next morning mid-routine, or quietly
  recreated `0th/n/`. After any structural change, grep the old path across skill, rules
  file and HANDOVER.

## Compression, three passes — each one found something broken
Cutting forces a read of every line, which is when stale content surfaces.
- `wiki-update` 5,305 -> 3,521 (66%; prose alone 45% off). Found: a check still expecting
  three `.md` at the space root when the flattened 0th made it four.
- `HANDOVER.md` 8,126 -> 6,976 (86%). A fact ledger resists compression and that is honest —
  never drop a claim to hit a number. Found: "Notion inbox is EMPTY, so nothing distilled
  yet", false since that morning. Structural win was deleting `## Morning routine`, which
  restated the skill verbatim.
- `my_wiki/CLAUDE.md` 2,912 -> 1,876 (64%). Seven facts lived in both it and the skill.
  Found: the file claimed "three exceptions" to "never a .md outside n/" while **omitting
  itself** — there are four, and the skill's check already expected four. The rules file had
  been in breach of its own rule.

## Verified end to end
`claude mcp list` -> notion connected. `fetch self` -> workspace `144_my_wiki's Space`.
Login through nginx/TLS: `0th/queue` 200, `1st/n/2026-08-25_SEO` 200, `index_1st` 200;
old `0th/n/queue` and the 15 deleted `seo-*` pages 404; no cookie 401, so auth is on.
Ledger agrees with disk both directions. No `.md` outside `n/` beyond the four exceptions.
No dead `[[links]]`. webUI untouched, `/` 200.

## Corrected the record
`HANDOVER.md` and `.claude/log/2026-08-24.md` both said syncthing had no folder shared and
no device paired. False — `noteryu` paired, folder `4qu9r-ehg2a` sendreceive on both
devices. Fixed in HANDOVER, marked as a correction in the 08-24 log.

Earlier logs still say `0th/n/queue.md`. Left alone on purpose: that was true when written,
and rewriting a log to match the present is how it stops being evidence.

## Moved late in the session
User moved `log/` into `.claude/log/`, so the pre-work plans and the project checklist now
live at `.claude/log/work_plan/`. Repo-root `log/` was left empty and is removed.
`HANDOVER.md:12` still named the old path — **third time today a hand edit to the tree
silently invalidated a file that referenced it** (after the 0th flattening broke the skill,
and the exception count disagreeing across three files). Standing rule: after moving
anything, grep the old path across HANDOVER, the skills and the rules file.

HANDOVER now *points at* the checklist rather than copying it — same reason
`## Morning routine` was deleted earlier: duplicated content drifts.

## Restructured my_wiki into a self-contained project
Mirrors `webUI/`, which the repo already treats this way.
```
my_wiki/
  CLAUDE.md            project rules (private)
  .claude/             HANDOVER, log/work_plan, skills/wiki-update
  my_wiki_vault/       the SilverBullet space (was my_wiki/)
    CLAUDE.md          space rules - public, served by SB
  servers/             was my_wiki_servers/
```
`compress` stays at repo root — general-purpose, also used on webUI-scope files.

**Stopped syncthing before touching anything.** Folder `4qu9r-ehg2a` is paired with
`noteryu`; moving the directory while it runs makes it see every file vanish and propagate
the deletions to the notebook. Order: stop syncthing + SB -> move -> repoint
`~/.local/state/syncthing/config.xml` -> restart. Verified afterwards: **0 deletion events
in the log**, 8 files intact, `.stfolder` present, reconnected to the notebook.

**Nix needs git.** `nix run` failed on the moved flakes — "not tracked by Git". Flakes only
see tracked files, so a `git add` is part of moving a flake, not an afterthought.

Also updated: `flake.nix` servers_init, `silverbullet/flake.nix` space path, `.gitignore`
(`my_wiki/my_wiki_vault/*` + negation — still `/*`, never `/`), the skill's internal paths
(now stated as repo-root-relative, since cwd stays `/home/opc/nix`), and both HANDOVERs.

Root HANDOVER now says `webUI/` **and** `my_wiki/` are self-contained; the wiki sections
moved to `my_wiki/.claude/HANDOVER.md`.

**Fourth stale-path find of the day.** After the move, `HANDOVER.md:44-45` still said
`cd my_wiki_servers/...` and the vault's own CLAUDE.md still pointed at
`../my_wiki_servers/`. Only the grep caught them. The standing rule holds: after moving
anything, grep the old path everywhere.

## Left undone
- **CHANGED cannot fire.** Needs a "Last edited time" property on the inbox database. One
  click in Notion, then Phase A gets it free.
- SEO links out to `/p/29b7ef98...`, outside the grant; its child toggles ("my strategy",
  "seo_keywords.md", "seo_") came back empty. Not fetched, not distilled.
- The distilled SEO page overlaps `webUI/.claude/refs/SEO_ref.md`. Reconciling them is a
  2nd-level job, deliberately not done at 1st.
- SilverBullet keeps no server-side page index, so pages written straight to disk need a
  browser reload/reindex before they appear. The user's client still listed deleted pages.
- Empty `<folder id="" label="" path="">` junk entry in syncthing's config.xml. Needs a
  restart to remove.
- No reboot survival, linger still on, support-bundle link still 404s.
- graphify: on the user's checklist, never specified, no design.
