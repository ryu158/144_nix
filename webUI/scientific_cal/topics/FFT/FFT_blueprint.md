0. common
0-1. style has to have consistency for whole webUI project
0-2. main tool is served by dev_basic
0-3. SEO optimization & anlytics tools have to set for blog & cal pages (later)
0-3-1. SEO tags live in the raw html, static: a crawler does not run js
0-3-2. internal links use public urls, never the topic html path, every public url in sitemap
0-4. acceess to advanced page has to be under payment (later)
0-5. spec.json owns every value a page shows: title, description, dataset, range, method list
0-5-1. never hardcode any of them in a page, the method list included
0-6. source material: ./fft.zip, built outside this repo
0-6-1. its own css, its own nav row and any cdn script tag get dropped, the page wears the site's
0-6-2. ./260829_my_webUI_FFT.md is the same text, earlier draft: keep whichever is newer, delete the other


1. blog
1-1. What is the Fourier Transform
1-2. Methods

1-2-1. Naive DFT
1-2-2. Cooley-Tukey radix-2
1-2-3. Mixed-radix / split-radix
1-2-4. Rader
1-2-5. Bluestein / chirp-z
1-2-6. Which one runs when

** have to be same under structure for each category of method
1-2-*-1. In plain words
1-2-*-2. Basic idea
1-2-*-3. Important characteristics
1-2-*-3-1. Advantages
1-2-*-3-2. Disadvantages

1-3. A note on the frequency grid: resolution, Nyquist, aliasing, leakage, windowing
1-3-1. these are DFT properties, not properties of any one algorithm, say so
1-4. REFs section: sources listed
1-5. figures: one per claim, generated by script, never hand edited
1-6. headings: header owns the only h1, article sections start at h2


2. cal
2-1. sampling: sample rate fs, or a time column
2-2. input: grid
2-3. output: grid
2-4. chart plot
2-5. method: fft only, naive DFT as the second entry (decide)
2-5-1. the blog's 5 algorithms are how a library dispatches, not a user choice
2-5-2. naive DFT is the only one worth exposing: same answer, visibly slower, proves the point
2-6. engine: ts based, client-side engine (no back-end) (decide)
2-6-1. if a client-side fft is too much, this level goes back-end and cal/adv split on features instead
2-7. no upload: this page computes in the browser, the claim must stay true

2-2-1. 1col for t or index, max 3 col for signal
2-2-2. copy & paste
2-2-3. default sample

2-3-1. no edit allowed except but copy to memory
2-3-2. output is a spectrum, not a resampled signal: col 0 = frequency, then magnitude per signal

2-4-1. chart can be generated in pop-up window
2-4-2. legend can be displayed or not
2-4-3. x axis is frequency, not time: input plot and output plot do not share an axis
2-4-4. y axis linear or dB (decide)

2-6-2. input must be uniformly sampled, that is the whole precondition of the transform
2-6-3. non-uniform or unsorted t: refuse with a message, never silently resample
2-6-4. skip a point when its t or its value is blank or not a number
2-6-5. a blank inside a series breaks uniformity: refuse that series, do not fill the hole
2-6-6. real input: report only the bins that carry information, up to Nyquist


3. adv
3-1. sampling: sample rate fs, or a time column
3-2. input: grid
3-3. output: grid
3-4. chart plot
3-5. method: fft, plus naive DFT for comparison
3-6. python back-end for every method: one path, no client-side math on this page
3-7. this page sends data to a server: say so, never reuse the cal "no upload" wording

3-2-1. 1col for t, no limit for col # for signal
3-2-2. copy & paste + import/export csv/tsv
3-2-3. default sample (optimized for displaying leakage and aliasing)

3-3-1. no edit allowed except but copy to memory
3-3-2. output columns: frequency, magnitude per signal, phase per signal (decide: phase on or off)

3-4-1. chart can be generated in pop-up window
3-4-2. legend can be displayed or not
3-4-3. user can choose which colums to display in chart (later)
3-4-4. user can set the color pallette (later)
3-4-5. user can set 'parallel computing' or 'GPU' for a big data set (later)
3-4-6. log or linear frequency axis, linear or dB magnitude axis

3-6-1. same cleaning and uniformity rules as cal 2-6, so the two pages agree on the same table
3-6-2. never sit silent on a failed request: running, failed, backend down all shown

3-8. window function: rectangular, Hann, Hamming, Blackman
3-8-1. the default is a real choice and changes every number shown, name it in the ui
3-9. zero-padding to a chosen length (later)
3-10. inverse transform back to the signal (later)


4. api
4-1. one POST endpoint per method, JSON in, JSON out
4-2. one backend for the whole site, not one per topic: a new topic is one module + one line
4-2-1. FFT is the second topic: it must add no new service and no nginx work
4-3. limits: max rows, max cols, max output bins, request body cap
4-3-1. public endpoint taking an arbitrary array, a size cap is not optional
4-3-2. naive DFT is O(N^2): its own row cap, lower than the fft one
4-4. binds loopback only, nginx is its whole public face
4-5. stateless: nothing written to disk, no user data in logs
4-6. errors: readable message, never a stack trace
4-6-1. non-uniform sampling is a 4xx with a reason, not a 500


5. validate
5-1. test_in_data.md / test_out_data.md in the topic folder
5-2. paste in, diff out, match within tolerance
5-3. never by eye
5-4. the fixture is a known signal: a pure sine on a bin centre gives one non-zero bin
5-5. naive DFT and fft must agree on the same input, that is what makes 2-5-2 honest


6. open decisions, resolve before building
6-1. 2-5: does the method box list algorithms at all, or only fft vs naive DFT
6-2. 2-6: client-side fft on the basic page, or back-end from the start
6-3. 2-4-4 and 3-3-2: dB axis, phase column
6-4. slug and public urls: /fft_blog, /fft_cal, /fft_adv, while the folder is topics/FFT today
