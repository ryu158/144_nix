# 2026-08-15

## Session: repo inventory vs. CLAUDE.md's target architecture (report only)

Read `CLAUDE.md`, looked for `.claude/rules/` and `.claude/skills/new-topic/SKILL.md`
(neither exists), and inventoried the actual repo against the target `src/shell/` /
`src/kit/` / `topics/<slug>/` structure CLAUDE.md describes.

Key findings:
- No Astro, no TypeScript build, no `package.json`, no npm scripts, no Python fixture
  pipeline anywhere — the CLAUDE.md Stack section was entirely aspirational. The real
  site is hand-written static HTML with vanilla JS loaded via `<script src>` tags.
  `flake.nix` still describes a Flask/NumPy/SciPy app (`app.py`) that doesn't exist.
- `dev_basic/` is the closest thing to `src/kit/` (shared `grid.js`/`chart.js`/
  `dual-chart.js`/`style.css`, genuinely reused). `prj/interpolate/` is the one real
  topic, but as loose HTML+JS rather than `spec.json` + `blog.md` + `calculator.ts`.
- Invariant violations already in the codebase: hardcoded nav in `index.html`,
  hardcoded colors in `bmc.js`/ko-fi widgets, no fixture-verified math test for
  `interp_engine.js`, per-page SEO meta duplicated instead of shell-driven, and —
  sharpest one — live AdSense (`dev_basic/ads.js`) and Buy-Me-a-Coffee/Ko-fi code
  already wired into pages despite CLAUDE.md's "Deferred — do not implement"
  monetization section.
- `Setup.md`, `Kit.md`, `Skill.md`, `Topics.md` at repo root are unfinished bootstrap
  scaffolding — `Setup.md` says to move them into `.claude/rules/` and
  `.claude/skills/new-topic/` and then delete itself, but that move was never done.
  Also found `Kit.md` and `Topics.md` are byte-identical (a bug in the scaffolding:
  `Kit.md` should govern `src/kit/`/`src/shell/` but duplicates the topics rules).

No changes made this session — inventory only.

## Session: corrected CLAUDE.md Stack and Environment sections

Per the inventory above, rewrote just those two sections to match reality — no
framework, no bundler, no build step, vanilla JS served from `dev_basic/` and
`prj/<topic>/`, no npm scripts to run. Noted that `flake.nix` still provisions
Python/Flask/SciPy and Node/TypeScript that nothing currently uses. Left every other
section of `CLAUDE.md` (Layout, Invariants, Visual grammar, etc.) untouched per
instruction, even though Layout still describes the not-yet-built `topics/` +
`blog.md`/`calculator.ts` architecture — that mismatch is now visible instead of
buried under a false Stack section.

## Session: migrated the interpolation topic to topics/interpolation/

Moved the one real topic out of `prj/interpolate/` into the target `topics/<slug>/`
layout, without inventing a build pipeline that doesn't exist yet:

- `git mv` of `interpolate_blog.html`, `interpolate_cal.html`, `interp_engine.js`,
  `page.js`, and the orphaned `test_data.csv` (748 rows, zero references anywhere in
  the repo — confirmed via search) into `topics/interpolation/`, filenames unchanged.
- User explicitly chose to keep the article/demo as `.html`/`.js` rather than convert
  to `blog.md`/`calculator.ts`, since nothing compiles or serves those formats today.
- Fixed the now-stale root-relative `/prj/interpolate/...` links and `<script src>`
  tags inside both HTML files (self-link, cross-links between blog and calculator,
  and the `interp_engine.js`/`page.js` script tags) to point at
  `/topics/interpolation/...`. Relative links to `/dev_basic/*` needed no change
  (same folder nesting depth). Root `index.html`'s only interpolation link points at
  the external `duckdns.org` production URL and was untouched.
- Added `topics/interpolation/spec.json`, extracted from currently-hardcoded values:
  `title`/`description` from the blog page's `<title>`/meta description, `levels`
  from which pages actually exist (`blog`, `calculator` — no `advanced`),
  `parameters` from the calculator's hardcoded `outputXMin`/`Max`/`Interval` defaults
  and the single `linear` method option. `dataset` left empty (no default dataset is
  hardcoded in the demo — the grid starts blank; `test_data.csv` is unused and not
  wired into it). `category`, `difficulty`, and `insight` have no literal source in
  the code — filled with best-effort placeholders flagged for the author to confirm.
  `socialImage` omitted (no OG image exists for this topic).
- Explicitly not done: no `src/kit/` (nothing has a second use yet), no
  `fixture.json`, no changes to `dev_basic/`, `prj/test/`, or root `index.html`.

Verified: `git status` shows clean renames, no leftover `/prj/interpolate/` string
anywhere under `topics/interpolation/`, `spec.json` is valid JSON. Not yet verified:
an actual browser click-through of the moved pages (grid/chart/interpolate button
still working end to end from the new path).
