User-agent: *
Allow: /

# The raw topic HTML duplicates the pretty URLs
# (/topics/interpolation/interpolate_cal.html == /interpolate_cal).
# Canonical tags point at the pretty URLs; this stops the duplicates being crawled.
Disallow: /topics/*/*.html

# Do NOT block /dev_basic/, /src/, /home.js or the topic JSON.
# Google renders pages before indexing them: blocking CSS/JS breaks that render,
# and blocking topics.json/spec.json would leave the homepage topic list empty
# to the crawler, cutting the internal links to every topic page.

Sitemap: https://ryuora144.duckdns.org/sitemap.xml
