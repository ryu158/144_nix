# 2026-08-28

Session: reform every Claude-config .md. No site code touched.

## What drove it
1. All rules loaded on every message, whether relevant or not.
2. CLAUDE.md carried 58 items. Most were topic-only or chart-only.
3. HANDOVER.md broke its own rule — roughly a quarter of it was copied from CLAUDE.md, rules/topics.md or future_work.md.

## CLAUDE.md — two passes
1. Pass 1, shape only: prose to numbered items under ## headers. 56 -> 84 lines. Reported plainly that it GREW; prose only fell 2%.
2. Pass 2, on user's "too many items": moved topic-only rules out to path-scoped rules files instead of deleting them.
3. Result 56 -> 62 lines, 3644 -> 2584 chars. 58 items -> 40.
4. Deleted outright, 2 only: types/globals.d.ts location, the src/shell/ file list. Both answered by ls.

## New path-scoped rules files
1. `rules/visuals.md` — the 8 visual-grammar items. Scope `topics/**` + `dev_basic/**`.
2. `rules/tests.md` — the 4 rules the Playwright suite depends on. Scope `tests/**`.
3. `rules/topics.md` gained topic folder shape, topic CSS placement, blog writing rules.
4. Mechanism was already in the repo — `paths:` frontmatter, same as rules/topics.md always had.

## HANDOVER.md
1. 9225 -> 6321 chars. Cut 11 units that another file already owned, moved 5 to rules/tests.md.
2. §Reality deleted whole — every line of it was in CLAUDE.md.
3. Test-spec inventory deleted: a count that rots, and ls answers it.
4. Kept every scar: DO NOT REMOVE verification tag, nginx.conf master-copy path, two-input flake rationale, ${self} trap, stale-shell trap, downloads-never-into-webUI.

## Owner conflict settled
1. future_work.md and HANDOVER "Not done" both listed favicon, MS Clarity, test_data.csv.
2. Split by definition — known gap in shipped work goes to HANDOVER, unapproved idea goes to future_work.
3. favicon and test_data.csv -> HANDOVER. MS Clarity stays in future_work.
4. The `.gitignore` wrong-case cause moved from HANDOVER to future_work with the rest of that item.

## log/ and refs/ — reform mostly refused, on purpose
1. Neither folder auto-loads. No `paths:` frontmatter, so trimming them saves zero context per session.
2. HANDOVER:3 names log/ the owner of history. Reform's cut rules would gut a log by design, because a log IS a dated snapshot.
3. The logs hold the causes behind the rules — uBlock's no-op gtag surrogate, the missing <title>, `${self}`, the .gitignore case. Cutting those keeps the rule and loses the why.
4. So: shape skipped, one genuine cut only. log/2026-08-21.md §"SEO reference built from 4 talks" 26 lines -> 10; it re-explained content that refs/SEO_ref.md owns.

## Defect found
`refs/SEO_ref.md:201` credited the dwell-time note to future_work.md. It actually lives in `rules/topics.md` §Analytics events. Pointer corrected.

## Self-review caught two real losses
1. The favicon fix path — needs an icon decision, then a static file plus <link rel="icon"> — nearly died while moving between files. Restored.
2. The fixture test being IN A BROWSER got flattened to a bare cell count. That is the whole point of it. Restored.
3. Method: script-diffed every sentence of both originals against the new file set, then checked all 83 mismatches by hand. 81 were rewordings or named cuts.

## Time sink worth remembering
User's editor held a stale buffer for several turns and showed 84 lines while disk held 62. Settled by searching every CLAUDE.md on the box and proving none was 80 lines. If a file "did not change", check the editor before re-editing the file.

## Committed
1. Branch `docs/reform-claude-config`, commit f315b4c, 8 files, +238 -200.
2. NOT merged to main yet — user's call.
3. `../.claude/skills/reform/SKILL.md` left alone. Outside webUI/, and already dirty before the session.
