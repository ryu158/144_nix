# 2026-09-04

Session: read yesterday back, then CSV/TSV import and export on the advanced
page. Built with two export buttons, then reworked so the format is chosen in
the save dialog instead. Suite 85 -> 98, all green.

## Started with a wrong diagnosis

The opening check found `/api/health` returning 502 on both localhost and the
public domain, no process matching app.py, nothing on port 35910, and uptime of
71 days. I reported the backend as down and offered to start it.

The user's answer was that the 502 had already been checked and solved. Both
things were true: the nginx route is correct and the service was started later
in the session, so what I measured was a snapshot, not a fault. Worth keeping
because the shape recurs — **a 502 on /api/ is nginx saying the route exists and
nobody answered.** It is never an nginx fault, and the handover already said so.

The user also said I had edited nginx.conf on misunderstood instructions. That
was the 2026-09-03 session, and its log already records those three files as
outside webUI scope and edited without a direct ask. Nothing was changed in
nginx this session; the block was only read to confirm the prefix behaviour.

## Explaining app.py, and the question that was actually interesting

Most of the morning was the user reading app.py and asking it back to me a piece
at a time. Two of their questions were sharper than the code's own comments:

**"How does app.run(host, port) become _run(topic, method)?"** It does not.
`app.run` is Flask's accept loop, entered once and never returned from.
`_run` is ours, called per request from inside it. The names collide and nothing
else does.

**"Step 4 has to come before 2 and 3, because without 4 there is no info about
topic and method."** This is the good one, and the answer is that there are two
different knowings. *Which* methods exist comes from the METHODS dict in the
source and is fixed at import. *Which one this caller wants* comes from the URL.
`_register` only needs the first, so it runs with the network unplugged.

Their mental model matches a real and valid alternative — `@app.route('/api/<topic>/<method>')`
with the values extracted per request. The repo took the loop instead so an
unknown name dies at routing rather than inside a handler, and so `app.url_map`
lists exactly what exists instead of a wildcard. Both are one line per new
method; the difference is where a bad name is caught.

The user wrote their own notes from that conversation as `webUI/app_py.md`.
Untracked, their file, not a rules file.

## CSV / TSV import and export

The advanced page could only take data in by paste and give it out by selecting
the output grid and copying. Anyone working from a file had no way in or out.

Four decisions were put up front and all four survived: import fills the input
grid and export saves the output grid; parsing matches grid.ts `_onPaste`
exactly; advanced page only, so it stays a first use under CLAUDE.md rule 4; and
no header row on export.

The parsing decision is the one worth defending. `_onPaste` sniffs tab-vs-comma
on the first line and splits, and it does not handle a quoted field containing a
comma. Copying that limitation deliberately keeps file and clipboard in
agreement. Fixing it in the import path alone would mean the same bytes give two
different grids depending on how they arrived.

Two existing mechanisms did the work that looked like it needed new code:
`setData` emits `change`, so re-plotting and the `demoPristine` clear happen
through handlers that already exist; and `setData` hard-resets dimensions, so an
import replaces the demo wholesale and needs no equivalent of the capture-phase
paste clear.

One real trap: `importFile.value` must be cleared BEFORE the await. Leave it set
and picking the same file twice fires no change event, so the second import
silently does nothing.

## Then the export design changed

Shipped first as two buttons, `export CSV` and `export TSV`, because a Blob plus
`<a download>` cannot ask the user anything — the browser writes the file with
whatever extension the code set.

The user asked for one button with the type chosen in the file explorer. That is
a different API: `window.showSaveFilePicker`, which opens a real Save-as dialog
with a type dropdown. It is Chromium-only, has no polyfill, and Playwright cannot
drive a native dialog. All three costs were stated before proposing it.

The user chose the fallback that loses nothing: one button everywhere, and on
Firefox and Safari a format `<select>` unhides beside it.

The inversion at the centre of this is worth stating plainly. **The format now
comes back from the handle, not from the page.** `showSaveFilePicker` returns
the name the user typed or picked, and its extension decides the delimiter. A
test asserts exactly that: the page never chose tsv, the dialog did.

Three details that are easy to get wrong:

1. The picker must be called with no `await` above it in the click handler.
   Chromium needs an unconsumed user gesture and one awaited tick spends it —
   after which the dialog silently never opens. `grid_2.getData()` is
   synchronous so this holds today, and the code says why.
2. `window.showSaveFilePicker` is read fresh on every click rather than captured
   at load, so a test can add or remove it per page.
3. AbortError means the user pressed Cancel. It is swallowed: no status change,
   no console error, and no `csv_export` event for a save that never happened.

`types/globals.d.ts` gained a minimal File System Access declaration — four
members, because TS 5.4's DOM lib ships none and the full spec types are far
more than the export path touches.

## Tests

85 -> 94 with the two-button version, then 94 -> 98 after the rework: 13 in
tests/interpolation/csv.spec.ts.

The stubbing is the interesting part. Playwright cannot open a native save
dialog, so the picker is faked per test — a handle named `out.tsv` or `out.csv`
whose writable pushes the written string onto the page, and `delete
window.showSaveFilePicker` for the fallback path, which is what Firefox and
Safari really look like. Both save paths are covered, plus cancel, plus the
empty-grid refusal asserting the dialog never opened at all.

One test earns its place more than the others: export, then re-import the
exported text, and assert the values survive. That is what the no-header
decision buys, and it would fail the moment a header row appeared.

## Own mistake worth remembering

Ran `npx tsc --noEmit` out of habit. `tsc` is not an npm dependency here — it
comes from the flake — so npm went and fetched an unrelated 12-year-old package
called `tsc` from the registry, printed "This is not the tsc command you are
looking for", and exited 0. The typecheck never ran and the exit code said fine.

Nothing was damaged: package.json and package-lock.json were unchanged and
node_modules had no new entry, both verified. But CLAUDE.md says no new
dependencies without permission, and `npx` will install one to satisfy a typo.
**Use the flake's `tsc` directly. `npx` is for Playwright and nothing else.**

## Committed and pushed

d2305bb, 10 files. Pushed to origin/main. Worth noting because it is easy to
forget: the git repo root is ~/nix, not webUI, so that push put app_py.md and
.claude/ onto a public GitHub repo. Already the known-and-accepted category,
same as CLAUDE.md and flake.nix, but it is the whole tree that travels, not the
subdirectory being worked in.

topics/FFT/fft.zip stayed untracked, as it has since 2026-09-02.

## Then the CSV code moved to dev_basic

The user's reasoning: other topics will need it. Mine: extracting with one
caller means designing the options object for a caller that does not exist,
which is the guess CLAUDE.md rule 4 exists to prevent.

I proposed making the second caller real first — wire /interpolate_cal, then
extract, so two live users force the shape. The user agreed with the
destination, then narrowed it: extract now, advanced page only, basic page
untouched. Their call, and it is coherent — the second user is FFT, not a second
page of this topic.

What was topic-shaped and where it went: the "interpolation_" filename prefix
and the hardcoded level:'advanced' became options; the two grids became options;
`method` is now read from #methodSelect if the page has one. Everything else was
already generic and moved verbatim, comments included.

**The extraction found a real bug that one caller cannot test.** setData hard-
resets numCols, so it walks straight past fixedColCount. A locked grid would
silently widen on import while a paste of the same file alerts and truncates —
and the basic page's grid is locked to 4 columns with a 10-column fixture
sitting in the repo. applyColumnLock now mirrors grid.ts, but the advanced grid
is unlocked, so no test reaches that branch. It ships documented and unproven,
which is worse than tested and better than silent.

Analytics changed shape too: events now carry `slug` as well as `level`. A
shared component firing an event that named only the level could not say which
topic produced it.

## Buttons lost their labels, on purpose

The user asked for plain "import" and "export", with the formats explained in
the manual instead.

The thing worth checking before agreeing: seo.spec.ts asserts the calculator's
visible body text contains "csv", "tsv" and "copy and paste". Both pages carry
all three in the how-to <summary>, which stays visible when the panel is closed,
so the button labels were never what backed that check. Verified, then changed.
A new test now asserts the labels read "import" and "export", so putting the
formats back is a decision rather than drift.

## Blueprints, and being corrected on my own document

Merged interpolation_cal_blueprint.md and interpolation_adv_blueprint.md into
one interpolation_blueprint.md, added a blog section that had never existed, and
deleted the two.

The user then rewrote it and asked me to compare. Theirs was better, for three
reasons worth keeping:

1. **Mine had drifted into map territory** — "drop the nav.spec.ts exemption",
   "fixture test_in_data.md carries 10 columns". Those describe work already
   done, not work requested. This is the exact failure the 2026-09-03 log
   records me making, one day later, in the same document family.
2. **Level-parallel numbering.** Their cal 2-x lines up against adv 3-x item for
   item, so the difference between the two levels reads by scanning one column.
   Mine said "same as basic except where stated", which makes a reader hold two
   documents at once.
3. **A per-method section template**, 1-2-*-N: In plain words / Basic idea /
   Important characteristics / Advantages / Disadvantages. Mine had nothing like
   it, and it is the single most reusable idea in either file.

What theirs dropped and I put back as sub-items: static SEO tags, public urls
plus sitemap, spec.json owning every displayed value, input cleaning, no-
extrapolation-to-blank, the entire api part, and the validate fixture. Each is a
requirement, not as-built detail, and each has already cost something once.

Two of their lines were decisions rather than wording, and both were put to them
rather than silently kept or silently changed:

1. "linear uses the client-side logic" on the advanced page. That would split
   the calculation into two paths and undo the reason the 0.000e+00 agreement
   check exists. Resolved: every method goes to the API.
2. A payment gate on the advanced page, which contradicts CLAUDE.md's
   no-monetization rule outright. Resolved by adding a Deferred line saying a
   paid tier is under consideration and not approved, so the two files stop
   disagreeing.

Their copy is kept, untouched, at their instruction. The two files now differ by
exactly the folded-in constraints.

## FFT blueprint

Written from 260829_my_webUI_FFT.md and the contents of fft.zip. The zip was
listed and piped, never extracted: nginx serves the repo root, so anything
unpacked there is public the moment it lands.

It holds a finished blog — fft_blog.md, fft_blog.html, 8 SVG figures,
gen_figs.py, build_html.py — the same shape interpolation.zip arrived in.

The blueprint is not interpolation's with different math, and the differences
are the content:

1. Output is a spectrum, not a resampled signal. Column 0 is frequency, and the
   output chart shares no axis with the input plot.
2. Uniform sampling is the transform's precondition. Non-uniform t is refused
   with a reason, never silently resampled — the same fact that kept FFT off the
   interpolation advanced page.
3. A blank inside a series breaks uniformity, so that series is refused rather
   than hole-filled. Interpolation's "skip the bad point" rule would be wrong
   here.
4. The window function is a first-class control: its default changes every
   number on screen, so the UI has to name it.
5. Naive DFT needs its own row cap, lower than the fft one, because it is
   O(N^2).

**The honest part is section 6.** The blog's five algorithms are how scipy
dispatches, not a user choice — putting "Rader" in a method box would be a lie
the page tells. Only fft-versus-naive-DFT is observable. That, whether the basic
page can carry a client-side FFT at all, the dB axis and phase column, and the
capital-F folder name are all listed as unresolved rather than answered by me.

## Not committed

The dev_basic/csv.ts extraction, both blueprints, and the CLAUDE.md line are in
the working tree.
