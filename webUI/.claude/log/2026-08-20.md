# 2026-08-20

Session: clean up Claude config files. No site code touched.

## Read first
All 4 logs + HANDOVER.md + rules + skills + Claude.local.md. State matched HANDOVER: interpolation done, SEO live, nginx fixed, GA4 works, no open bug.

## Dead stuff deleted
- `.claude/hooks/block-prj-dev-basic.sh` + `.claude/hooks/` — guarded `prj/dev_basic/`. `prj/` deleted 2026-08-14, so the hook could never fire. Cost a bash+jq spawn on every Write/Edit for nothing.
- `.claude/settings.json` — the hooks block was its only key. No `settings.local.json` exists.
- `.claude/skills/component-summary/` — maintained `dev_basic/summary/*.md`. That folder never existed; none of its 4 named reference files existed. Fully dead since written.
Grepped first: nothing outside those files (and the logs) referenced them.

## Skill was broken, not just stale
`.claude/skills/new-topic/SKILL.md` had NO `---` frontmatter fences. `name:`/`description:` never parsed — the skill list showed `new-topic: name: new-topic`. Undiscoverable since it was written 08-18. Fixed by fencing. Confirmed live: description now renders.

## Triplication killed
CLAUDE.md + rules/topics.md + SKILL.md were 238 lines repeating the same facts (spec.json shape, static SEO head block, register-in-topics.json, CSS fallback, validation). Log 08-19 already recorded all three drifting together once.
New split — each fact has one owner:
- `CLAUDE.md` (50 lines) — always loaded. Repo-wide rules only: style, scope, stack, map, HARD RULES, visual grammar, writing, deferred. Two pointer lines out to the skill and the rules file. Workflow + SEO how-to + spec fields removed.
- `.claude/rules/topics.md` (87 lines) — loaded under `topics/**`. Sole owner of the spec.json block, the static head block, SEO validate order, links/sitemap, body-text-carries-terms, SEO targeting, analytics events. Cut the CSS section (dup of a CLAUDE.md hard rule) and the "no fixture.json" negations (nothing left to negate). Fixed stale `"name": "Linear Interpolation"` -> `"Interpolation"`.
- `.claude/skills/new-topic/SKILL.md` (39 lines) — 6-step checklist only, points at rules/topics.md instead of restating.
Names still appear in more than one file, but only as a one-line rule or a checklist step. No block is duplicated.

## Claude.local.md was committed
`.gitignore` said `CLAUDE.local.md`; real file is `Claude.local.md`. Case mismatch on a case-sensitive FS -> tracked all along, though its own first line claimed "uncommitted, gitignored".
Fixed `.gitignore` case, `git rm --cached Claude.local.md`. File still on disk, now actually ignored.
NOT fixed, stated only: it stays in already-published git history. Added to future_work.md.
